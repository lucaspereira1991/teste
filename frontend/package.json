{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Hello World TypeScript Frontend",
  "scripts": {
    "build": "tsc && echo Build complete!",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
